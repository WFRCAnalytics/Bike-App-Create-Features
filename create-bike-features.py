# load roads features

# subset roads where (BikeFeatureLeft != Null or '') or (BikeFeatureRight != Null or '')

# subset roads where 'BIKE_PLN_L IS NOT NULL Or BIKE_PLN_R IS NOT NULL'



# load trails features

# 'DesignatedUses NOT IN ('Pedestrian') And Status NOT IN ('Future', 'Proposed', 'PROPOSED') And CartoCode NOT IN ('1 - Hiking Only', '7 - Steps')'